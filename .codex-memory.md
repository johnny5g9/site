# Codex Long-Term Memory

- Keep a workspace-local memory file present during tasks, per AGENTS.md.
- Website social preview preference (2026-03-09): homepage link previews should use title "Groissl Hockey Photography" and show the logo image.
- Homepage intro preference (2026-03-19): use an opening logo animation with a liquid fade-in and drip-away reveal before the site content appears.
- Homepage intro refinement (2026-03-19): the liquid effect should read as the actual logo melting/dripping, not a generic liquid overlay.
- Homepage intro refinement (2026-03-19): avoid recoloring the logo during the drip; use actual logo imagery/slices so the transition stays smooth and recognizable.
- Homepage intro refinement (2026-03-19): the effect should read like the logo bottom is softening and stretching downward before separate drips form.
- Homepage intro refinement (2026-03-19): switch from dripping to a dust blow-away if the melt effect fights the shape of the logo; remove old drip-only code when making that change.
- Homepage intro refinement (2026-03-19): dust version should dissolve from top to bottom into tiny black particles, and the intro backdrop should be slightly gray so the particles stay visible.
- Homepage intro refinement (2026-03-19): if the CSS dust effect still feels fake, switch to a custom canvas-based pixel dissolve that samples the real logo and releases very fine dark particles from the top down.

- Preference (2026-03-11): Only change the GitHub-backed site unless explicitly asked otherwise, and deploy after each requested site change.
- Workflow preference (2026-03-25): push after every requested site change unless the user says not to.
- Repo note (2026-03-17): the git-tracked deployable site files are currently the root `index.html`, `script.js`, and `styles.css`; the `/site` folder is present as an untracked duplicate copy.
- Git note (2026-03-17): `origin/main` is the established branch for the existing site history; `origin/master` was created separately and should not be used as the primary deploy branch.
- Historical UI preference trail (2026-03-09): business-name placement around the header changed several times; if this comes up again, check prior homepage/header decisions before editing.
- Historical UI preference (2026-03-09): sticky header should shrink on scroll rather than disappear, and the compact state should be slightly smaller than the first revision.
